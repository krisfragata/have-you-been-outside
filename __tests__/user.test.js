//import necessary packages such as supertest and server
const request = require('supertest');
const app = require('../back-end/server');

//test if user login checks database and routes to sign up if user doesn't exist

//test if user is created at signup